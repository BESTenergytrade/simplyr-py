[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "simplyr_py"
version = "0.0.1"
description = "Python wrapper of the simplyr-lib market matching algorithms"
readme = "README.md"
license = "MIT"

